import { Box } from '@material-ui/core';
import styled from 'styled-components';

export const MessageListLine = styled.li`
  margin-bottom: 20px;
`;

export const MessageList = styled.ul``;

export const MessageBox = styled(Box)`
  background-color: '#ffd580';
`;
